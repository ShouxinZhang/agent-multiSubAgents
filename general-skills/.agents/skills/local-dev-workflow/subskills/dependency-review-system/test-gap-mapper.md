# test-gap-mapper

## 目标

把风险项映射为最小补测清单，缩短人工决策到落地修复的距离。

## 当前形态

1. 当前无独立 MCP tool（文档型步骤）
2. 建议基于 `review_validate_llm` 结果中的 `parsed.advisories` 执行映射

## 建议输出格式

1. 风险项 -> 建议测试类型（单测/集成/回归）
2. 风险项 -> 最小可执行测试场景
3. 风险项 -> 预期覆盖的失败模式
